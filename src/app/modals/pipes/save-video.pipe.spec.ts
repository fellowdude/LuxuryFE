import { SaveVideoPipe } from './save-video.pipe';

describe('SaveVideoPipe', () => {
  it('create an instance', () => {
    const pipe = new SaveVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
