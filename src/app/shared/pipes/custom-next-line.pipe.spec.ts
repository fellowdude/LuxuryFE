import { CustomNextLinePipe } from './custom-next-line.pipe';

describe('CustomNextLinePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomNextLinePipe();
    expect(pipe).toBeTruthy();
  });
});
