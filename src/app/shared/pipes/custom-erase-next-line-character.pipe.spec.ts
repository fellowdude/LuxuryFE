import { CustomEraseNextLineCharacterPipe } from './custom-erase-next-line-character.pipe';

describe('CustomEraseNextLineCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomEraseNextLineCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
