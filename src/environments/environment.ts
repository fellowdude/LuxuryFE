// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  //URL_BACKEND: 'http://204.48.18.206:3000',
  // URL_BACKEND: 'https://9405-190-235-94-191.ngrok.io',
  //URL_BACKEND: 'https://uat.backend.luxury.pe',

  URL_BACKEND: 'https://backend.luxury.pe',
  // URL_BACKEND: 'https://backend2.marchablanca.luxury.pe',
  // URL_BACKEND: 'http://localhost:3000',
  GTAG: 'UA-203868702-A',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
